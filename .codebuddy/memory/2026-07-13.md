# 2026-07-13

## plan-mode / 自主引擎 收尾验证（engine-ux-verify）
- 全量 `npm run ci` 通过（EXIT=0）：format:check + lint + type-check + test:node + test:coverage 全绿。
- 修正 `components/PlanPanel.tsx` prettier 格式（提交 `65751ae`），使 ci 通过。
- 两轴 code-review（本环境 general-purpose 子代理不可用，改为手动两轴审查）：
  - **Standards**：`@/` 别名纪律达标——`lib/agent-orchestrator/*.test.mjs` 无 `@/` 亦无 `./index` 引用（纯 Node 可解）；持久化与 logger 为纯 fs 模块、不依赖 pi SDK；i18n 走 `lib/i18n/zh.ts`；中文注释。唯一可争议点：`orchestrations.jsonl` 与 `engine-runs.jsonl` 两持久化模块近重复（不同记录形状/上限/文件，属合理分治，非硬违规）。
  - **Spec**：原始 6 项需求全部落地（计划模型配置/混合总控/按角色模型/Token 优化/双系统持久化/结构化日志）+ UX（配置区·节奏指示·Token 徽标·日志历史·引擎日志视图·rediscuss 增量重议）。
- 当前分支 `phase1-2-engineering-security` 领先 `origin` 2 个提交（feature `6890129` + format `65751ae`），尚未 push（push 需用户批准）。

## 提交前严格验证 + push（用户显式要求）
- 执行严格验证评审后已 push 成功：`2b11700..65751ae  phase1-2-engineering-security -> origin`。
- 验证项：① 全量 `npm run ci` 复跑仍 EXIT=0；② **额外跑 `npm run build`（CI 不含）** 验证所有 API 路由顶层导入无报错（含 `setOrchestratorRunnerFactory` 触发的 `@/lib/pi` 链路），BUILD_EXIT=0，全部 31 文件/新增路由编译通过；③ 逐模块逻辑审查（orchestrator/controller/runner/llm-backend/双 persistence/plan-mode-config/unified-engine-runtime）无缺陷；④ 路由接线齐全：`/api/plan/[id]/{select,confirm,events,rediscuss,log}` 与 `/api/engine/{changes,runs,stream,history,log}` 均存在；⑤ i18n 键 `plan.*` 与 `engine.*` 在 `zh.ts`/`en.ts` 齐全（zh 以 `satisfies TranslationKeys` 强制对齐）；⑥ `engine-logger.log()` 永不抛出、`readLogFileTail` 文件缺失/损坏返回 `[]`，不会误判讨论失败。
- 结论：功能逻辑符合需求、运行无报错、测试全绿，可安全 push。

## 代码重复提取重构（共用项复用）
- 识别重复集群：① 6+ 个 `*Config.tsx`/`*Panel.tsx` 完全重复的 modal 外壳（overlay/header/sidebar/footer）、② 列表行 hover+选中背景逻辑复制 ~20+ 次、③ SaveButton 的 saving/savedOk 勾选动画、④ 19+ 处 `fetch(... csrfHeaders({...}) + JSON.stringify + res.json()`。
- 新增**共用模块**（纯 UI/工具，行为中立、低风险）：
  - `components/ui/ConfigModal.tsx`：`ConfigModal`(外壳) / `ConfigSidebar` / `ConfigListRow`(选中+hover) / `ModalButton`(primary/secondary/danger) / `SaveButton`(带 saved-pop 动画)。
  - `lib/csrf-fetch.ts`：`csrfFetchJson(url, {method, body, headers})` → 返回 `{ok, status, data}`。
  - `lib/api-utils.ts`：新增 `jsonOk(data, init)` 统一 JSON 成功响应。
- **完整重构示范**：`components/SkillsConfig.tsx` 改用上述共用项（modal 外壳/ConfigListRow/ModalButton/csrfFetchJson），移除全部重复 boilerplate，行为不变。`app/api/settings/route.ts`、`app/api/models-config/route.ts` 改用 `jsonOk`。
- 校验：`npx tsc --noEmit`、`eslint`、`prettier --check` 对相关文件均 0 error/0 warning。
- 滚动推广计划（未执行，待用户确认）：其余 7 个配置面板（Models/Plugins/Mcp/Extensions/Agents/Settings/WebSearch）套用同一套 `ConfigModal` 族；62 个 API 路由统一走 `safeJsonBody`+`validateCsrf`+`errorResponse`+`jsonOk`。

## 配置面板重构推广（用户确认"是的"后执行）
- 将 `csrfFetchJson` 全量落地到 **6 个面板**：ModelsConfig / PluginsConfig / ExtensionsConfig / McpConfigPanel / AgentsConfig / WebSearchConfigPanel（取代 `csrfHeaders`+`JSON.stringify`+`fetch`+`res.json()`）。其中 ModelsConfig、PluginsConfig 额外套用 `ConfigListRow`/`ModalButton`/`SaveButton` 原语（保留各自既有的正确遮罩外壳，零回归局部提取，未整体重写 return 块）。
- 修复 3 个类型错误：ModelsConfig import 位置修正、`AgentsConfig` `d.optimized ?? ""`、`ExtensionsConfig` 显式泛型 `csrfFetchJson<{id?,error?}>`。
- 应一致性要求，额外将 **SettingsPanel.tsx** 与 **PluginConfigPage.tsx** 的 mutation/加载 fetch 也改用 `csrfFetchJson`（SettingsPanel 三处 GET+POST 全部替换，移除 `csrfHeaders` 导入）。
- 校验：`npx tsc --noEmit` 0 error；`eslint` 0 error（仅 13 个预存 warning：ModelsConfig SSE 登录回调 12 处 `!` 非空断言 + PluginConfigPage 预存未用 `field` 参数，均非本次引入，warning 不阻断 `eslint .`）；`prettier --check` 全绿。
- 范围边界：非配置面板的 `csrfHeaders` 使用（ChatInput/SessionSidebar/SessionItem/PinCurrentDirButton/EnvProvisionButton/PinnedDirsList/PlanPanel 共 18 处）**未纳入**，留作后续，避免范围蔓延与回归风险。

## 非配置面板 csrfHeaders 推广（用户二次确认"是"后执行）
- 将 `csrfFetchJson` 落地到 7 个非配置面板共 16 处 `csrfHeaders` 用法：SessionItem(2: PATCH 改名/DELETE 删除)、PinCurrentDirButton(1: 固定目录增删)、EnvProvisionButton(1: mcp env 扫描)、PinnedDirsList(2: 取消固定/保存别名)、ChatInput(3: 计划编排/rediscuss/enhance)、SessionSidebar(4: cwd 校验/默认 cwd/worktree 增删)、PlanPanel(6: 含 3 处既有 csrfHeaders + 3 处无 csrfHeaders 的 GET，统一消除该重复模式)。
- 行为等价要点：`csrfFetchJson` 内部 `res.json().catch(()=>{})` 兜底；原 `res.ok`/`data.error`/`data.path!`/`d.enhanced` 等判断与字段用法逐处保留（enhance 的 `d.enhanced` 为空时 `"".pad` 改 `?? ""`）。
- 校验：`npx tsc --noEmit` 全项目 0 error；`eslint` 7 文件 0 error（仅 6 个预存 warning：ChatInput `planConfig` 依赖/`!`/a11y，SessionSidebar 3 处 `!`，均非本次引入）；`prettier --check components/**` 全绿；`grep csrfHeaders components/` 残留 0。
- 至此 components/ 目录内 **已无裸 `csrfHeaders` + fetch 反模式**，全部走 `csrfFetchJson`。
