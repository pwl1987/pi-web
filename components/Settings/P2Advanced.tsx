/**
 * P2Advanced — 高级设置面板（调试 / 实验性 feature flag）。
 * ponytail: P1 阶段占位；Step 4.6 后期根据需要落地。
 */

"use client";

export function P2Advanced() {
  return (
    <div style={{ padding: 12, color: "var(--text-muted)", fontSize: 12 }}>
      高级设置 — P1 占位，后续迭代。
    </div>
  );
}
